*py_os_other.txt*

			Random - 随机数
------------------------------------------------------------------------------
						*py-os-random* 

*urandom* 		

For example:
>
  # 获取随机数
  os.urandom(16)
<
------------------------------------------------------------------------------


			Miscellaneous - 其他系统信息
------------------------------------------------------------------------------
						*py-os-cpu_count* 

*cpu_count* 		

For example:
>
  # 获取cpu核心数
  os.cpu_count()
<
------------------------------------------------------------------------------
						*py-os-name* 

*os.name*
*os.uname()*
------------------------------------------------------------------------------
						*py-os-id* 
*os.getegid*	当前进程有效组ID
*os.geteuid*	当前进程有效用户ID
*os.getgid*		当前进程实际组ID
*os.getuid*		当前进程实际用户ID
*os.getpid*		当前进程ID
------------------------------------------------------------------------------
 vim:tw=78:ts=8:ft=help:norl:
